# Progress Ledger — Phase 1: Wordlist + uebungssatz-03

Plan: docs/superpowers/plans/2026-07-04-phase1-wordlist-und-uebungssatz.md
No git repository in this project (by user decision) — tasks are tracked here instead of via commits.

Task 1: complete (scaffold + raw extraction). Review: Approved. Note: raw file has CRLF line endings; Task 2's parser handles this via `split(/\r?\n/)` — confirmed already in plan, no fix needed. Plan's Task 1 Step 5 expected line numbers were wrong (217-219 -> corrected to 637-639, content/order were always correct); fixed in plan + brief.
Task 2: complete (extract-wordlist.ts + 6 tests). Review: Approved, byte-exact transcription confirmed via diff. Watch-items for Task 3 spot-check: (a) wortart classification untested by the suite, (b) missing-article detector only fires with an inline plural marker present -- a bare capitalized noun without one would silently merge into the previous entry's beispiele instead of warning. Not fixed (accepted heuristic tradeoff per spec's known-limitations), just flagged for the human spot-check step.
Task 3: complete (real parser run + spot-check). Verification: `node scripts/extract-wordlist.ts` parsed 683 entries and 1 warning; warning is `Satz, -ä, e Dieser Satz ist sehr einfach.` as expected. Spot-check confirmed `danken`, `der Dank` with two examples, `danke`, and `Beispiel`/`zum Beispiel` output are internally sensible.
Task 4: complete (check-vocab.ts + 2 tests). Verification: `node --test "scripts/__tests__/check-vocab.test.ts"` passed 2/2; `node --test "scripts/__tests__/**/*.test.ts"` passed 8/8.
Task 5: complete (render-exam.ts + 2 tests). Verification: `node --test "scripts/__tests__/render-exam.test.ts"` passed 2/2; `node --test "scripts/__tests__/**/*.test.ts"` passed 10/10. Note: renderer uses `path.dirname()` instead of the plan snippet's slash-only regex so CLI output works on Windows paths.
Tasks 6-9: complete (full `content/exams/uebungssatz-03/exam.json`). Verification: JSON parses; Hören has 6+4+5 scored items and two examples where expected; Lesen has 5+5+5 scored items and one Teil 3 example; Schreiben has 5 form fields and 3 Inhaltspunkte; Sprechen has 7 Teil 1 prompts, two 6-card topics, and 2 Bildkarten.
Task 10: complete (render exam Markdown). Verification: `node scripts/render-exam.ts` rendered `content/exams/uebungssatz-03/exam.md` and `content/exams/uebungssatz-03/loesungen.md`; candidate sheet contains no `Lösungen`/`loesung`/`richtig`/`falsch`/`Musterlösung` leakage; answer key includes expected Hören/Lesen lines plus Schreiben fields and Musterlösung.
Task 11: complete (vocabulary checker run and final verification). Verification: final `node --test "scripts/__tests__/**/*.test.ts"` passed 10/10; `node scripts/extract-wordlist.ts` parsed 683 entries and 1 warning; `node scripts/render-exam.ts` regenerated Markdown; `node scripts/check-vocab.ts` produced 211 heuristic review flags, mostly inflections/proper names/compounds/form labels. Simplified `Meeting`/`Laptop` to `Termin`/`Brief` after checking the parsed wordlist.
